export default class User {
  constructor(first_name, last_name, username, password, bank, bank_number, know_me, user_ufabet, password_ufabet) {
    this.first_name = first_name,
    this.last_name = last_name,
    this.username = username,
    this.password = password,
    this.bank = bank,
    this.bank_number = bank_number,
    this.know_me = know_me,
    this.user_ufabet = user_ufabet,
    this.password_ufabet = password_ufabet
  }
}
